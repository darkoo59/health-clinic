<Window x:Class="Sims_Hospital_Zdravo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sims_Hospital_Zdravo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button  Grid.Column="0" Name="Patient" Click="Patient_Click" Width="100"  VerticalAlignment="Center">Patient CRUD
        </Button>
        <Button Grid.Column="1" Name="Secretary"  VerticalAlignment="Center" HorizontalAlignment="Center" Width="100" Click="Secretary_Click" >Secretary CRUD
        </Button>
        <Button Grid.Column="2" Name="ManagerBtn" Click="Manager_Click" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center" >Manager CRUD
        </Button>
        <Button Grid.Column="3" Name="DoctorBtn" Click="Doctor_Click" Width="100"  HorizontalAlignment="Center" VerticalAlignment="Center" >Doctor CRUD
        </Button>
    </Grid>
</Window>
